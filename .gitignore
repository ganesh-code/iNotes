# Environment (secrets live only in root .env)
.env
.env.local
.env.*.local

# Generated at dev/build from root .env or AWS secrets
client/public/config.js

# Local script overrides
scripts/mongodb.config.json

# OS / editor
.DS_Store
